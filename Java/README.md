# Java

    Primitive:
      int
      float
      double
      char

    Costrutti:
      if (condizione)
      var = (condizione) ? True : False
      while(Condizione)
      Do-While(Condizione)
      For(condizione)
      For(Lista : Obj)
   
     OOP:
       Class Name
       getter - setter
       interface
       extends
       enumerate
       try - catch(Exception)
       throws Exception
  
    Lambda:
      Predicate
      Consumer
  
    Swing:
      JLabel
      JSeparator
      JButton
      JTextField
      JTextPassword
      
    Jar esterni:
        JSONSimple
        GSON
    
     
